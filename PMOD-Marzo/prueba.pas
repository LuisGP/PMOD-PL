program hola;
{A Autores}
{Programa de prueba válido}
;{** JAJA}{P}{R 0}procedure procedimiento(a,b,c: Integer; z: char);
begin
	c := a + b
end;
;{** MUAJAJA}{P}{R nada}function funcion(a,b,c: Integer): Integer;
	;var
		v: Real;
	;type
		rec = record
			a: integer;
			end;
	;procedure proc;
	begin
		a := 0
	end;
begin
	funcion := a + b + c
end;
;function f1(a:Integer):Integer;
begin
	f1 := 1
end;
;function f2:Integer;
begin
	f2 := 0
end;

;{** CORTO}const
	a = 2;
	whle = 45;
	rojo = 'rojo';
;var
	a, b, h: Integer;
	z: String;
	luis: Persona;
;type
	rec = record
		a: Integer;
		end;
	rec2 = record
		b: Integer;
		end;
	rec3 = record
		g: String;
		end;
begin
	a := 5+6;
	cd := a;
	;var
		f: integer;
	begin
		f := 'BLOQUE ANONIMO!!'
	end;
	df := df;
	re := ((45*78+id)=6)and not(not a or b);
	while A <> a do
	begin
		a := 8 + 2 = 2 and 3 * 2 <> 8 or not (2 + 3 = 4);
		re := re + 3
	end;
	procedimiento(a + 2 - 3, 5 * 2 + 2, b);
	b := funcion(h - 2, f1(2+3), f2)
end.